Thank you for installing {{ .Chart.Name }}.

Your release is named {{ .Release.Name }}.

-----------------------------------------------------------------------------------
To Access Your Release, Run The Following:
-----------------------------------------------------------------------------------

    host=$(kubectl get ingress -n {{ .Values.namespace }} | grep {{ .Values.namespace }} | awk '{print $3}')

    curl -H "Host: $host" http://$(minikube ip)/
    OR
    curl -H "Host: $host" -k https://$(minikube ip)/

-----------------------------------------------------------------------------------