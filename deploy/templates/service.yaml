apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.appName }}
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Values.appName }}
spec:
  ports:
  - name: http
    port: 9999
    protocol: TCP
    targetPort: 8000
  selector:
    app: {{ .Values.appName }}
  type: LoadBalancer